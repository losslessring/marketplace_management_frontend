DONE Create store for nodeData

DONE On dragging, whe mouse runs over the node, it catches its relative coordinates
when using offsetX and offsetY, and the dragging frame resets and blinks.
Idea to avoid this: check the target id, if it is different fron canvas, or,
better from id, that was initially on drag start.
If it is changed, i should use the last drag value. The frame will stop,
but it is still better than blinking on reset.

DONE On dragging, when dragging backwards, the mouse event offsetX and offsetY catches itself as a target and the frame coordinates resets and the frame blinks. Fixed it by adding some offset.

DONE Fix the node data. When selecting the node, its data value is added to node data store even if it is not saved in database yet.
No need to fix. Node data is inialized with some data, when selecting.

DONE Node data is not changable in a new node.

PARTIALLY Getting error when trying to return object or array from the code in a text area.
Probably the problem is with promises

When pressing Save Graph button, all graph and all node data should be saved.

Implement code nodes without JSON.stringify/JSON.parse
Probably here is the hint to the solution
https://stackoverflow.com/questions/3710204/how-to-check-if-a-string-is-a-valid-json-string
Node should check if the data is the object and act differently if JSON parsing is failing
on undefined or [Object] object

Create a larger input field for code and JSON objects

